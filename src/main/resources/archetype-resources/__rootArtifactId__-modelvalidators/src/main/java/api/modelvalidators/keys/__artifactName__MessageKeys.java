#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
package ${package}.api.modelvalidators.keys;

/**
 * Project message keys for jsr303 custom validators
 *
 * @author
 */
// Using interface so it works with the error key generator for display
// CHECKSTYLE:OFF
public interface ${artifactName}MessageKeys {
	// CHECKSTYLE:ON

	/* Example of a message key constant:
	static final String ${artifactNameUpperCase}_SPECIFIER_INFO_ERROR = "${artifactNameLowerCase}.specifier.info.error";
	*/
}
